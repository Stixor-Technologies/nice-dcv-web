/** @type {import('next').NextConfig} */

// const path = require("path");

const nextConfig = {
  reactStrictMode: true,
  // webpack(config) {
  //   config.resolve.alias["dcv"] = path.resolve("public/dcvjs/dcv.js");
  //   return config;
  // },
};

module.exports = nextConfig;
